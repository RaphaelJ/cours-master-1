package network.protocol.actions;

import network.protocol.*;

/** Is used as a base interface for every action message from the client to the
 * server. */
public interface ActionMessage extends Message {
}
