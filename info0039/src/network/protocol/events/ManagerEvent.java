package network.protocol.events;

/** Is used as a base interface for every event message from the server to the
 * client which signal a change in the global state of the game. */
public interface ManagerEvent extends EventMessage {
}
